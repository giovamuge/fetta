import type { Config } from "prettier";

const config: Config = {
	useTabs: true,
	tabWidth: 4,
	semi: true,
	singleQuote: false,
	trailingComma: "es5",
	printWidth: 75,
	plugins: [],
};

export default config;
